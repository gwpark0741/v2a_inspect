Select TTA or VTA for each audio event segment below.

**Routing Policy:**

→ Choose TTA (Text-to-Audio) when:
  - Sound is continuous or atmospheric (engine hum, ambient noise, sustained music)
  - Precise frame-by-frame sync is NOT required
  - Multiple independent sources benefit from clean isolation
  - Source is clearly visible with predictable, steady motion

→ Choose VTA (Video-to-Audio) when:
  - Strict frame-by-frame sync required (fast impacts, lip-sync, fight scenes)
  - Sound source is NOT visible on screen (source_visible=off-screen: gunshots, unseen footsteps)
  - Rapid/unpredictable motion that must be captured in sync

**Segments to evaluate:**
{segment_list}

Watch the video at each time range. For each segment, reason step-by-step then decide.

Return ONLY valid JSON:
{{"segments": [{{"segment_index": 0, "reasoning": "step-by-step reasoning here", "selected_model": "TTA", "confidence": 0.85}}]}}
